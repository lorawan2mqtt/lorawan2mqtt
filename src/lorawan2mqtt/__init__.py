"""lorawan2mqtt — decode LoRaWAN sensor payloads into clean MQTT / Home Assistant data.

Early preview: the USB bridge is functional (`lorawan2mqtt --port ... ` or
`python -m lorawan2mqtt.usb_bridge`); the community codec database and the
rest of the project live at https://github.com/lorawan2mqtt/lorawan2mqtt.
"""

__version__ = "0.0.2"
